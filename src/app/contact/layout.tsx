import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Bite Instruments | Wholesale Grooming Shears Supplier",
  description: "Get in touch with Sialkot's leading professional grooming shears manufacturer. Contact us for bulk quotes, custom OEM styling packages, and branding requests."
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
